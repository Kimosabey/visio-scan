# VisioScan

Multimodal RAG — drawings and field photos via vision-language model (Ollama).

**GitHub:** `Kimosabey/selfaware-visio-scan` — after `gh auth login`, from this folder:

`gh repo create Kimosabey/selfaware-visio-scan --private --source=. --remote=origin --push`

Planned API port: **8105**.
